settings:
  git:
    branch: master

media:
  input: public/images
  output: /images

content:
  - name: posts
    label: 博客文章
    type: collection
    path: src/content/posts
    subfolders: true
    format: yaml-frontmatter
    view:
      fields: [title]
      primary: title
    fields:
      - name: title
        label: 文章标题
        type: string
        required: true

      - name: published
        label: 发布时间
        type: date

      - name: date
        label: 日期(备用)
        type: date

      - name: description
        label: 文章摘要
        type: text

      - name: image
        label: 封面图片
        type: image

      - name: tags
        label: 标签
        type: list
        field:
          type: string

      - name: category
        label: 分类
        type: string

      - name: draft
        label: 是否草稿
        type: boolean
        default: false

      - name: body
        label: 正文内容
        type: rich-text

  - name: spec_pages
    label: 特殊页面
    type: collection
    path: src/content/spec
    subfolders: true
    format: yaml-frontmatter
    view:
      fields: [title]
      primary: title
    fields:
      - name: title
        label: 页面标题
        type: string
        required: true

      - name: body
        label: 页面正文
        type: rich-text

  - name: public_files
    label: 静态文件上传
    type: collection
    path: public
    fields:
      - name: file
        label: 选择上传文件
        type: file